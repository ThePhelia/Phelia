# --- Web / ASGI ---
fastapi>=0.111,<0.115
uvicorn[standard]>=0.30
gunicorn>=23.0

# --- DB / Migrations ---
SQLAlchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9

# --- Pydantic / settings ---
pydantic>=2.7,<3
pydantic-settings>=2.2

# --- Auth / crypto ---
python-jose[cryptography]>=3.3
# passlib + bcrypt: фиксируем версии, чтобы не ловить баг "module 'bcrypt' has no attribute '__about__'"
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- HTTP / feeds ---
httpx>=0.27
feedparser>=6.0.11

# --- Tasks / broker ---
celery>=5.4
redis>=5.0

# --- Utils (по желанию, но полезно) ---
python-dotenv>=1.0
orjson>=3.10

